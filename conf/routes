# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /healthcheck                           controllers.Application.healthcheck()

POST     /users/:id/events/alarms                controllers.Application.addAlarm(id : String)
GET      /users/:id/events/alarms                controllers.Application.getAlarms(id : String)


