# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /healthcheck                           controllers.Application.healthcheck()

POST     /users/:id/events/alarm                controllers.Application.addAlarm()

